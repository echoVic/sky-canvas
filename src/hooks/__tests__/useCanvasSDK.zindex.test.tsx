/**
 * useCanvasSDK Z-index 相关测试
 *
 * 注意：此文件暂时禁用，等待 CanvasSDK 接口稳定后重新启用
 */

import { vi } from 'vitest';

describe.skip('useCanvasSDK Z-index 测试', () => {
  // 暂时跳过所有测试，直到接口稳定

  it('should be implemented later', () => {
    expect(true).toBe(true);
  });
});